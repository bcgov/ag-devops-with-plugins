# Generated by ag-devops v2.0.0 — scaffold.py --type sast-ci
# DO NOT EDIT MANUALLY — re-run scaffold.py to regenerate
name: SAST and Secrets Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sast:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: bcgov-c/ag-devops/ci/security/sast@main
        with:
          project-key: my-proj
          sonar-token: ${{ secrets.SONAR_TOKEN }}
          sonar-host-url: https://sonar.example.com
          sources: .
          coverage-report: coverage/lcov.info
          github_token: ${{ secrets.GITHUB_TOKEN }}
          gitleaks_license: ${{ secrets.GITLEAKS_LICENSE }}
