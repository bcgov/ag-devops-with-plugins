# Generated by ag-devops v2.0.0 - scaffold.py --type docker-ci
# DO NOT EDIT MANUALLY - re-run scaffold.py to regenerate
name: Docker Build and Push

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker:
    uses: bcgov-c/ag-devops/.github/workflows/docker.yml@main
    with:
      image: my-app
      registry: ghcr.io/bcgov-c
      context: .
      file: Dockerfile
      platforms: linux/amd64
      auto_tagging: "true"
      push: ${{ github.event_name == 'push' && 'true' || 'false' }}
      scan: "true"
    secrets:
      registry_password: ${{ secrets.GHCR_TOKEN }}
      registry_username: ${{ secrets.GHCR_USERNAME }}
