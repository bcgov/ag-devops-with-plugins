# Generated by ag-devops v2.0.0 — scaffold.py --type externalsecret
# DO NOT EDIT MANUALLY — re-run scaffold.py to regenerate
{{/*
  ExternalSecret — integrates with Vault SecretStore on OpenShift Emerald.
  Requires the external-secrets operator to be installed in the namespace.

  Add to your values.yaml:
  myApi:
    externalSecret:
      secretStoreName: vault           # the SecretStore/ClusterSecretStore name
      keys:
        - remoteRef: secret/path/MY_SECRET
          secretKey: MY_SECRET
*/}}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.project }}-my-api
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: my-api
    app.kubernetes.io/part-of: {{ .Values.project }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.myApi.externalSecret.secretStoreName | default "vault" }}
    kind: SecretStore
  target:
    name: {{ .Values.project }}-my-api
    creationPolicy: Owner
  data:
  {{- range .Values.myApi.externalSecret.keys }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        key: {{ .remoteRef }}
  {{- end }}
