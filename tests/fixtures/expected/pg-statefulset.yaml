# Generated by ag-devops v2.0.0 — scaffold.py --type statefulset
# DO NOT EDIT MANUALLY — re-run scaffold.py to regenerate
{{- $p := dict "Values" .Values -}}
{{- $_ := set $p "ApplicationGroup" .Values.project -}}
{{- $_ := set $p "Name" "pg" -}}
{{- $_ := set $p "ServiceName" "pg" -}}
{{- $_ := set $p "ModuleValues" .Values.pg -}}
{{- $_ := set $p "Ports" "pg.ports" -}}
{{- $_ := set $p "Probes" "pg.probes" -}}
{{ include "ag-template.statefulset" $p }}

{{- define "pg.ports" -}}
- name: http
  containerPort: 5432
  protocol: TCP
{{- end }}

{{- define "pg.probes" -}}
livenessProbe:
  httpGet: { path: /health/live, port: http }
  initialDelaySeconds: 10
  periodSeconds: 30
readinessProbe:
  httpGet: { path: /health/ready, port: http }
  initialDelaySeconds: 5
  periodSeconds: 10
{{- end }}
