---
name: ag-init
description: "Initialize a new application repository for OpenShift Emerald deployment. Asks 8 questions about your project then runs the init-emerald-repo script to generate .github/workflows (CI + CD), gitops/ Helm chart, per-environment values files, Makefile, CODEOWNERS, .gitignore, and a project-root AGENTS.md. One command — fully automated, no copy-paste."
---
Use the `init-emerald` agent to fully initialize this repository for OpenShift Emerald deployment.
