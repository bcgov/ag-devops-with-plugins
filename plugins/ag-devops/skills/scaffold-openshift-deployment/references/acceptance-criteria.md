# Acceptance Criteria: scaffold-openshift-deployment

## Functional Requirements

- Skill triggers on the correct prompts (see evals/evals.json)
- Provides guidance for complete Chart.yaml, values.yaml, and all resource templates
- References the `ag-helm-templates` library chart from GHCR OCI

## Output Requirements

- Complete chart structure guidance with example templates

## Non-Goals

- Does not execute scripts
- Does not write files
